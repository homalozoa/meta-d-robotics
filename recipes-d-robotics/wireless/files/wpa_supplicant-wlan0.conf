# SPDX-License-Identifier: Apache-2.0

# No network credentials are embedded in the image.  Configure wlan0 with
# wpa_cli and use `save_config`, or edit this conffile before deployment.
ctrl_interface=DIR=/run/wpa_supplicant
update_config=1
